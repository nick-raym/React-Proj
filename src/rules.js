export default function Rules(){
    return(
        <h3 id="rules">The rules of Plinko are simple. Contestants start by dropping their ball from one of the top slots and then watch as it bounces and falls its way down to the bottom. The slots at the bottom are labeled with different prize values, ranging from a small amount of points to large amounts.</h3>
    )
}